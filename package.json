{
  "name": "affinidi-core-sdk",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "ci": "lerna exec npm ci",
    "clean": "lerna clean",
    "tsc": "lerna run tsc",
    "link": "lerna link",
    "version": "lerna version",
    "lint": "lerna run --parallel lint",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap --ignore-scripts",
    "publish": "lerna publish",
    "test": "lerna run --parallel test",
    "test:coverage": "lerna run --parallel test:coverage",
    "test:coverage:codeclimate": "lerna run --parallel test:coverage:codeclimate",
    "exec": "lerna exec",
    "list-packages": "lerna list --json",
    "checkLicenses": "lerna run --parallel checkLicenses"
  }
}
