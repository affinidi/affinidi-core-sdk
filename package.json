{
  "name": "affinidi-core-sdk",
  "private": true,
  "devDependencies": {
    "lerna": "^5.5.0"
  },
  "scripts": {
    "ci": "lerna exec npm ci",
    "clean": "lerna clean",
    "tsc": "lerna run tsc",
    "link": "lerna link",
    "version": "lerna version",
    "lint": "lerna run --no-bail --parallel lint",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish",
    "test": "lerna run --no-bail --parallel test",
    "test:coverage": "lerna run --no-bail --parallel test:coverage",
    "test:coverage:codeclimate": "lerna run --parallel test:coverage:codeclimate",
    "exec": "lerna exec",
    "list-packages": "lerna list --json",
    "checkLicenses": "lerna run --no-bail --parallel checkLicenses"
  },
  "overrides": {
    "parse-url": "8.1.0"
  }
}
